Each tool needs to have own folder that describe what tool does and containing an index.ts file. The class also needs to describe what the tool does and have the prefix 'Tool' at the end.

for each tool you need to write tests via "@nestjs/testing"
